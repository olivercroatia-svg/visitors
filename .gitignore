node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
coverage/
.playwright-mcp/
client/dev-dist/
server/dist/
*.tsbuildinfo
.remember/

# HTZ eVisitor reference material — contains test-environment credentials.
# Local only: the repo is public.
API VISITOR/

# Fiscal signing certificates and keys. These are qualified certificates with legal
# effect — they live in the database (encrypted), never in the repo.
*.p12
*.pfx
*.pem
*.key
*.crt
